# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — RAGnarok Candidate Ranking System
# Python 3.11.2 · CPU-only · No GPU · No network during ranking
#
# Install: pip install -r requirements.txt
#
# POST-INSTALL STEPS (run once):
#   1. python -m spacy download en_core_web_sm
#   2. python precompute_llm.py        # downloads GGUF model
#   3. python build_indexes.py         # builds FAISS / BM25 / feature indexes
# ─────────────────────────────────────────────────────────────────────────────

# PyTorch CPU-only wheel index — must be declared here for `pip install -r` to work
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Core ML / NLP ─────────────────────────────────────────────────────────────

# Bi-encoder (all-MiniLM-L6-v2) + cross-encoder (ms-marco-MiniLM-L-6-v2)
sentence-transformers==3.4.1

# PyTorch CPU-only build — must install before sentence-transformers
torch==2.5.1+cpu

# HuggingFace Transformers — sentence-transformers dependency
transformers==4.47.1

# Tokenizers — required by transformers; exact pin prevents ABI mismatches
tokenizers==0.21.0

# ── Vector Search ─────────────────────────────────────────────────────────────

# FAISS CPU build — IVF256 index for dense similarity search
faiss-cpu==1.8.0

# ── Keyword Search ────────────────────────────────────────────────────────────

# BM25 sparse retrieval (rank.py → retrieval/keyword_path.py)
rank-bm25==0.2.2

# ── NLP Parsing ───────────────────────────────────────────────────────────────

# spaCy for JD parsing and noun-phrase extraction
# After install: python -m spacy download en_core_web_sm
spacy==3.7.4

# ── Numerical ─────────────────────────────────────────────────────────────────

# NumPy — FAISS arrays, feature store, trajectory DB
numpy==1.26.4

# ── Fast JSON ─────────────────────────────────────────────────────────────────

# orjson — fast streaming JSON/JSONL parsing in rank.py
orjson==3.11.9

# ── LLM Reranker ─────────────────────────────────────────────────────────────

# llama-cpp-python — CPU inference for Qwen2.5 GGUF model
llama-cpp-python==0.3.31

# huggingface-hub — GGUF model download in precompute_llm.py
huggingface-hub==0.36.2

# ── Progress ─────────────────────────────────────────────────────────────────

# tqdm — progress bars during pre-computation (candidate_parser.py)
tqdm==4.67.1

# ── Testing ───────────────────────────────────────────────────────────────────

pytest==8.3.5
pytest-cov==6.1.0